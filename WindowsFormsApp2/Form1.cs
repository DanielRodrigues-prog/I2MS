using System;
using System.Windows.Forms;

namespace WindowsFormsApp2 // Use o nome do seu projeto aqui
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            // Este método é gerado pelo designer e constrói a interface.
            // Não mexa nele manualmente.
            InitializeComponent();
        }
    }
}